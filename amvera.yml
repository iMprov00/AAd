meta:
  environment: ruby
  toolchain:
    name: bundle
    version: "3.2"
build:
  image: null
run:
  image: null
  mainScript: "app.rb"       # Указывает главный файл приложения
  persistenceMount: /data
  containerPort: "9292"       # Порт контейнера. Должен совпадать с настройкой в app.rb